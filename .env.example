# =============================================================================
# Brown's Mill Dentist - Environment Variables
# =============================================================================
# Copy this file to .env.local for development or .env.production.local for production.
# NEVER commit actual .env files to version control.
#
# Next.js automatically loads:
#   .env                - All environments
#   .env.local          - All environments (git-ignored)
#   .env.development    - Development only
#   .env.production     - Production only
#
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# =============================================================================

# Site URL (used for canonical URLs, OG tags, sitemap)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Contact information (public, used in UI)
NEXT_PUBLIC_PHONE_NUMBER=
NEXT_PUBLIC_EMAIL=
NEXT_PUBLIC_ADDRESS=
